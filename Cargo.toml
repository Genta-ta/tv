[package]
name = "search"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "search"
path = "src/search/main.rs"

[dependencies]
# JANGAN isi apa-apa di sini agar tidak memicu build openssl

[profile.release]
opt-level = 3
strip = true
lto = true
